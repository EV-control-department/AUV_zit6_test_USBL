sudo snap install micro-ros-agent
# 赋予权限（只需要做一次）
sudo snap connect micro-ros-agent:fortran-compiler
sudo snap connect micro-ros-agent:serial-port

# 启动（注意命令前缀）
micro-ros-agent serial --dev /dev/ttyUSB0 -b 115200
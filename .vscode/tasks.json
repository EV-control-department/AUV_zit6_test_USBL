{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Firmware",
            "type": "shell",
            "command": "cmake --build build",
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        },
        {
            "label": "Flash Firmware (OpenOCD)",
            "type": "shell",
            "command": "openocd -f interface/stlink.cfg -c 'transport select hla_swd' -c 'adapter speed 1000' -f target/stm32h7x.cfg -c 'program build/AUV_zit6.elf verify reset exit'",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
